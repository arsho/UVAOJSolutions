#include<cstdio>
#include<cstring>
#include<cassert>
#include<vector>
#include<list>
#include<queue>
#include<map>
#include<set>
#include<deque>
#include<stack>
#include<bitset>
#include<algorithm>
#include<functional>
#include<numeric>
#include<utility>
#include<sstream>
#include<iostream>
#include<iomanip>
#include<cmath>
#include<cstdlib>
#include<ctime>
#include<fstream>
#include<typeinfo>
#include<locale>
#include<iterator>
#include<valarray>
#include<complex>
using namespace std;

int main()

{
    long long int a,b[5],i,j,k;
    scanf("%lld",&a);
    
        for(i=1;i<=a;i++)
        {
            for(j=0;j<4;j++)
            {
                scanf("%lld",&b[j]);
            }
            sort(b+0,b+5);
            if((b[0]==b[1])&&(b[2]==b[3])&&(b[1]==b[3]))
            printf("square\n");
            else if((b[0]==b[1])&&(b[2]==b[3]))
            printf("rectangle\n");
            else if((b[0]+b[1]+b[2])>b[3])
            printf("quadrangle\n");
            else
            printf("banana\n");

        }
      
    
    return 0;
}
