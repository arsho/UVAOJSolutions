#include<cstdio>
#include<cstring>
#include<cassert>
#include<vector>
#include<list>
#include<queue>
#include<map>
#include<set>
#include<deque>
#include<stack>
#include<bitset>
#include<algorithm>
#include<functional>
#include<numeric>
#include<utility>
#include<sstream>
#include<iostream>
#include<iomanip>
#include<cmath>
#include<cstdlib>
#include<ctime>
#include<fstream>
#include<typeinfo>
#include<locale>
#include<iterator>
#include<valarray>
#include<complex>
using namespace std;
int main()
{
        int t,n,x[300],i,j,sum,dif,res;
        while(scanf("%d",&t)==1)
        {
                for(j=1;j<=t;j++)
                {
                scanf("%d",&n);
                for(i=0;i<n;i++)
                {
                        scanf("%d",&x[i]);
                }
                sort(x,x+n);
                sum=0;
                for(i=0;i<n-1;i++)
                {
                        dif=x[i+1]-x[i];
                        sum+=dif;
                }
                res=sum*2;
                printf("%d\n",res);
                }
        }
        return 0;
}
